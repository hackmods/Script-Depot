#set up git and initialize a project
sudo apt-get install git
git config --global user.name "SampleName"
git config --global user.email "SName@email.com "
git init && git add -A && git commit -m "Initializing Project"